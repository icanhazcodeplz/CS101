#!/bin/bash

# 1. Print your current directory to console (done for you as an example)
pwd
# 2. Create a new directory called 'test_folder'

# 3. Navigate into 'test_folder'

# 4. Print your current directory to console again

# 5. Create empty files named 'file1.txt', 'file2.txt', and '.hidden_file.txt'. (notice period in front of 'hidden_file')

# 6. List the contents of 'test_folder'. Use options to print hidden files.

# 7. Use echo and output redirection (either > or >>) to write the text "Hello, World!" into 'file1.txt'.

# 8. Append the text "The weather is nice today" as a new line to the end of 'file1.txt'

# 9. Repeat the previous step 2 times (3 total times)

# 10. Print the entire contents of 'file1.txt'

# 11. Print the first two lines of 'file1.txt'

# 12. Copy file 'file1.txt' to a new file called 'file1_backup.txt'.

# 13. List the contents of 'test_folder' with no options

# 14. Create a new directory called 'backups' as a subfolder of 'test_folder'

# 15. Move 'file1_backup.txt' into 'backups'.

# 16. List the contents of 'backups' with no options

# 17. List the contents of 'test_folder' with no options

# 18. Remove 'file1.txt'

# 19. List the contents of 'test_folder' with no options

# 20. Delete folder 'backups' and all of its contents recursively (should be a single command)

# 21. List the contents of 'test_folder' with no options

# 22. Create an environment variable called MY_VAR with the value CS101_Student

# 23. Print the new environment variable MY_VAR

# 24. Move up one directory (to where you started originally)

# 25. Delete 'test_folder' and all of its contents

# 26. Attempt to navigate into 'test_folder' (should fail)