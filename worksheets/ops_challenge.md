## Java Order of Operations - Challenge

**Name:** ___________________________________________

Evaluate each expression and write the final value of `result`. Show your work.

---

```java
int a = 14;
int b = 2;
boolean c = false;
boolean result = (a / b + 2 >= 7 - 1 || 3 * 4 <= 11) && !(a / b == 7 && a - b != 12) || c;
```

<div style="page-break-before: always;"></div>

```java
int x = 15;
int y = 4;
boolean z = true;
boolean result = x - 3 * y > 2 && (8 / y + 1 >= 4 || z) && !(x / 3 == 5 && 8 - y < 5) || 6 + 2 != 8;
```


